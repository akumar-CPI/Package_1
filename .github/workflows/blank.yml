name: CPILint Governance Check

on:
  push:
    branches:
      - main
    paths:
      - '**'  # Trigger on any file change

jobs:
  cpilint-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'  # Adjust based on CPILint requirements

      - name: Download CPILint relase
        run: |
          wget https://github.com/mwittrock/cpilint/releases/download/v1.1.0/cpilint-1.1.0.zip -O cpilint.zip
          unzip cpilint.zip -d cpilint
          chmod u+x cpilint/cpilint-1.1.0/bin/cpilint
          echo "$GITHUB_WORKSPACE/cpilint/cpilint-1.1.0/bin" >> $GITHUB_PATH

      - name: Run CPILint on changed files
        run: |
          cpilint -rules roless.xsd -files test.zip
